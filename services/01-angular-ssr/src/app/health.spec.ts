// Angular test: run `ng test --watch=false`
// health endpoints in server.ts tested via integration test against running server
describe('01-angular-ssr placeholder', () => {
  it('passes', () => expect(true).toBe(true))
})
